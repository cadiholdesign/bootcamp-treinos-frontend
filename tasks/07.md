## Task

Create the screen that is at https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=134-952&m=dev. BE 100% FAITHFUL TO FIGMA.

## Technical Requirements

- If the user does not have training data and/or an active training plan, take them to this new page. It must be in /onboarding. To retrieve this information, use the `getHomeData` and `getUserTrainData` functions that are in @app/\_lib/api/fetch-generated/index.ts. Perform this check on all pages.

- It must render @chat.tsx.

- In the top bar, place an "Access FIT.AI" button that takes the user to the home page.