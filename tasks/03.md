## Task

Create the screen that is at https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=66-254&m=dev. BE 100% FAITHFUL TO FIGMA.

## Technical Requirements

- Create the screen in @app/workout-plans/[id]/days/[id]/page.tsx.

- The user should be redirected to it when clicking on @app/\_components/workout-day-card.tsx.

- Call the `getWorkoutDay` function that is in @app/\_lib/api/fetch-generated/index.ts to retrieve the necessary data.

- If the user already has a training session in progress for the day that has not yet been completed (check this through the "sessions" object returned from the API), display the "mark as completed" button (see https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=66-254&m=dev and https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=66-255&m=dev). Now, if they do not have any sessions started, display the "Start workout" button (see https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=86-428&m=dev). This button should call the `startWorkoutSession` function. After calling `startWorkoutSession`, call `revalidatePath` in Next to reload the page.

When clicking the "?" button in the day's exercises (https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=86-489&m=dev) DO NOTHING for now.

The back button in the top bar should return to the previous page.

The button with the "Calendar" icon in @bottom-nav.tsx should be active when on that page. Clicking it should also take you to that page. To do this, in @bottom-nav.tsx also call the `getHomeData` function.

The page MUST NOT be a client component. Only transform the necessary components within it into client components.

- If a training session is completed for the day, display a ghost button in place of the "Start Training" button with the text "Completed!".