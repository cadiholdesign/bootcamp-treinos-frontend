## Task

Create the screen that is at https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=75-507&m=dev. BE 100% FAITHFUL TO FIGMA.

## Technical Requirements

- Create the navigation bar (https://www.figma.com/design/icjlmwVThvfpiYYXFld8gG/FIT.AI?node-id=75-572&m=dev) but the other buttons (except the home button) will not do anything.

- Create the screen in @app/page.tsx.

- This screen must be a server component.

- Retrieve the data using the `getHomeData` function that is in @app/\_lib/api/fetch-generated/index.ts.

- The "View history" button should do nothing for now.

- The "View workouts" button should do nothing for now.

- When clicking on today's workout, do nothing for now, but wrap it inside a <Link>.

- Create a component that renders the workout day to reuse it elsewhere later.

- The squares with the days of the week (Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, and Sunday) will be completely blue if the user has already completed a workout day on that day. Great! It will be half blue if the user has only started a workout day. And it will be completely white if the user has neither started nor completed a workout day. To obtain this information, along with today's workout, call the `GetHomeData` function. See the "consistencyByDay" object.